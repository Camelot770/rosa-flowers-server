meta:
  environment: docker
  toolchain:
    name: docker
    version: ""

build:
  dockerfile: Dockerfile
  skip: false

run:
  containerPort: 4000
  persistentVolumeClaimName: uploads-pvc
  persistentVolumeMountPath: /app/uploads
